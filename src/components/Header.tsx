"use client";
import { useState } from "react";import { Menu,Phone,X } from "lucide-react";import { brand,cta,navigation,phone } from "@/data/site";
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-black/8 bg-[#f7f4ee]/95 backdrop-blur"><div className="container flex h-[82px] items-center justify-between"><a href="#top" aria-label="ASHANA PORT — на главную"><strong className="serif block text-[27px] leading-none tracking-[.16em]">{brand.name}</strong><span className="mt-1 block text-[8px] tracking-[.21em]">{brand.subtitle}</span></a><nav className="hidden items-center gap-7 lg:flex" aria-label="Основная навигация">{navigation.map(x=><a key={x.href} className="text-[13px] font-semibold hover:text-[#556243]" href={x.href}>{x.label}</a>)}</nav><div className="hidden items-center gap-5 md:flex"><a href={`tel:${phone.replace(/[^+\d]/g,"")}`} className="flex items-center gap-2"><Phone size={17}/><span><b className="block text-sm">{phone}</b><small className="block text-right text-[10px] text-black/55">Заказать звонок</small></span></a><a className="btn btn-primary" href="#lead">{cta.estimate}</a></div><button onClick={()=>setOpen(!open)} className="grid size-11 place-items-center lg:hidden" aria-label={open?"Закрыть меню":"Открыть меню"} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container border-t border-black/10 py-5 lg:hidden" aria-label="Мобильная навигация">{navigation.map(x=><a onClick={()=>setOpen(false)} className="block border-b border-black/7 py-3 text-lg" key={x.href} href={x.href}>{x.label}</a>)}<a className="btn btn-primary mt-5 w-full" href="#lead" onClick={()=>setOpen(false)}>{cta.estimate}</a></nav>}</header>}
